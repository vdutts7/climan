_about: "shelljson compliance declarations for manpages skill scripts"
ref: "$SHELLJSON"
skill: manpages

rules_applied:
  SHELL-001: "shebang: #!/usr/bin/env zsh on all executable scripts"
  SHELL-004: "paths use $HOME not ~"
  SHELL-052: "no bare pip / --break-system-packages"
  SHELL-034: "strict mode: setopt ERR_EXIT PIPE_FAIL; set -euo pipefail in .sh"

scripts_in_scope:
  - scripts/shelljson-gate.sh
  - scripts/validate-skill-bundle.sh
  - scripts/shell-lock-pack.sh
  - tests/smoke.sh

notes:
  - "lookup.sh is the single execution entry; fetch.sh is the HTTP client cascade"
